This is the Feature based structure
Frontend React and JS (javascrpipt)

.Primitive values are copies the value itself not a reference linked
.non-primitive values are refrence based.
.Shallow copy and deepcopy
->Shallow uses the destructuring so the link effect there.
Ex:let newEmployee = { ...employee };
->deepcopy will completely gives the new object.
ex:JSON.parse(JSON.stringify(a));

.Mutate { Means it will change the exisiting value .Mutate = modify what already exists.}

. The key and value in object forms a property. We know both are key and value. or key and value called as the property.

. promise method -> this is an javascript method where it will call to the browser network.But when you do the js api calling it will immediatley returns a promise where after browser will using the network tab it will call to server then returns to promise .
The promise has 2 states resolve and reject when server response get the resolve then data . if not reject.

. The response obj data is inside the body so we need take that so we use the .JSON() to get that body data.
. response receives the parsed body -> res.json()

. when i manually use the "throw new Error" it will give the Error object.

.useState :
it will return an array with initial value and setter function.

2.from the starting i am good player in bgmi e sports were i want to go and record the nodwin tournament vlogging it will as the 30days i think.

I will post the every video of that ?

3.My mom said that i will move into governament exams preparation move to nalagonda and take 1 year of time done the job my age as the 25 years only
